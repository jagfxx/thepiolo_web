# Base de datos (PostgreSQL en la VPS, solo localhost)
DATABASE_URL="postgresql://thepiolo:CHANGE_ME@127.0.0.1:5432/thepiolo_billing?schema=public"

# Auth — generar: openssl rand -base64 32 (usa el mismo valor en ambas)
AUTH_SECRET="CHANGE_ME"
NEXTAUTH_SECRET="CHANGE_ME"
NEXTAUTH_URL="https://thepiolo.icu"

# Usuario admin inicial (npm run db:seed)
ADMIN_EMAIL="thepiolo.co@gmail.com"
ADMIN_PASSWORD="CHANGE_ME"

NODE_ENV="production"
